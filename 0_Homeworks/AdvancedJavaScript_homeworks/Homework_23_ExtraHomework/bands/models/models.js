export default class Band {
  constructor(name, isActive, tags, bandMembers, albumsNum) {
    this.name = name;
    this.isActive = isActive;
    this.tags = tags;
    this.bandMembers = bandMembers;
    this.albumsNum = albumsNum;
  }
}
